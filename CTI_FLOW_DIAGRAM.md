# CTI CID 연동 흐름도

## 🔄 전체 흐름 (단계별)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        CTI CID 연동 전체 흐름                             │
└─────────────────────────────────────────────────────────────────────────┘

    [1단계]                  [2단계]                  [3단계]
    ┌─────────┐             ┌─────────┐             ┌─────────┐
    │ CTI     │  POST       │ Next.js │  Socket.IO │ Socket.IO│
    │ Bridge  │ ──────────> │ API     │ ──────────>│ Server  │
    │ (C#)    │ /api/cti/   │ Route   │ emit()     │ (포트   │
    │         │ incoming-  │         │            │ 3002)   │
    └─────────┘ call        └─────────┘            └─────────┘
       │                                              │
       │ 실제 전화 발생                               │ 브로드캐스트
       │ CID 수신                                     │
       │ (발신번호, 수신번호)                         │
                                                        │
                                                        │ 'cti-event'
                                                        │ 이벤트 전송
                                                        ▼
    [4단계]                                          [5단계]
    ┌─────────┐                                     ┌─────────┐
    │ Socket.IO│ <───────────────────────────────── │ 브라우저│
    │ Client   │  WebSocket 연결                     │ (React) │
    │ (포트    │  (localhost:3002)                  │         │
    │  3002)   │                                     │         │
    └─────────┘                                     └─────────┘
         │                                                │
         │ 이벤트 수신                                     │ 상태 업데이트
         │ 'cti-event'                                    │ currentCall 설정
         │                                                ▼
                                                    [6단계]
                                                    ┌─────────┐
                                                    │ CTI Panel│
                                                    │ 화면 표시│
                                                    │         │
                                                    │ 전화번호 │
                                                    │ 표시됨!  │
                                                    └─────────┘
```

---

## 📊 상세 단계 설명

### 1단계: CTI Bridge에서 CID 수신
```
┌─────────────────┐
│  CTI Bridge     │
│  (C# 프로그램)   │
│                 │
│  전화 착신 발생! │
│  CID 수신:      │
│  발신: 010-xxx  │
│  수신: 031-xxx  │
└────────┬────────┘
         │
         │ HTTP POST 요청
         │ http://localhost:3001/api/cti/incoming-call
         │
         ▼
```

**발생 이벤트:**
- 실제 전화가 오면 CTI Bridge가 CID 정보 수신
- `PostToNode()` 함수 실행
- Next.js API로 POST 요청 전송

---

### 2단계: Next.js API에서 CID 처리
```
         │
         ▼
┌─────────────────┐
│  Next.js API    │
│  /api/cti/      │
│  incoming-call   │
│                 │
│  1. CID 데이터   │
│     수신         │
│  2. Socket.IO   │
│     서버 확인    │
│  3. CTI 이벤트   │
│     생성         │
└────────┬────────┘
         │
         │ global.io.emit('cti-event', ...)
         │
         ▼
```

**처리 내용:**
1. ✅ CID 데이터 받기: `{ callerNumber, calledNumber, timestamp }`
2. ✅ Socket.IO 서버 존재 확인: `global.io` 체크
3. ✅ CTI 이벤트 생성: `{ EventType: 'INCOMING_CALL', PhoneNumber: ... }`
4. ✅ Socket.IO로 브로드캐스트: `global.io.emit('cti-event', ctiEvent)`

**중요 포인트:**
- ❌ `global.io`가 없으면 브로드캐스트 실패 (경고 로그 출력)
- ✅ `global.io`가 있으면 브로드캐스트 성공

---

### 3단계: Socket.IO 서버에서 브로드캐스트
```
         │
         ▼
┌─────────────────┐
│  Socket.IO      │
│  Server         │
│  (포트 3002)    │
│                 │
│  연결된 클라이언트│
│  확인            │
│  브로드캐스트    │
│  'cti-event'    │
└────────┬────────┘
         │
         │ WebSocket
         │ 연결된 모든 클라이언트에게 전송
         │
         ▼
```

**처리 내용:**
1. ✅ 연결된 클라이언트 수 확인: `io.sockets.sockets.size`
2. ✅ 모든 클라이언트에게 `cti-event` 전송
3. ⚠️ 연결된 클라이언트가 없으면 로그만 출력 (전송 실패)

**중요 포인트:**
- 브라우저가 연결되어 있어야 이벤트를 받을 수 있습니다
- 연결된 클라이언트가 0명이면 아무도 받지 못합니다

---

### 4단계: 브라우저에서 Socket.IO 연결
```
         │
         ▼
┌─────────────────┐
│  브라우저       │
│  (React 앱)     │
│                 │
│  Socket.IO      │
│  클라이언트      │
│  연결           │
│  localhost:3002 │
└────────┬────────┘
         │
         │ useCTI() 훅
         │ 연결 및 이벤트 리스너 등록
         │
         ▼
```

**처리 내용:**
1. ✅ Socket.IO 클라이언트 연결: `io('http://localhost:3002')`
2. ✅ 연결 성공 확인: `socket.on('connect')`
3. ✅ CTI 세션 참가: `socket.emit('join-cti-session', { userId })`
4. ✅ `cti-event` 리스너 등록: `socket.on('cti-event', ...)`

**중요 포인트:**
- 페이지 로드 시 자동으로 연결 시도
- 연결 실패 시 오류 로그 출력

---

### 5단계: 브라우저에서 이벤트 수신 및 상태 업데이트
```
         │
         ▼
┌─────────────────┐
│  useCTI 훅      │
│                 │
│  'cti-event'    │
│  수신           │
│                 │
│  상태 업데이트:  │
│  - currentCall  │
│  - events       │
│  - callHistory  │
└────────┬────────┘
         │
         │ React 상태 업데이트
         │ setState()
         │
         ▼
```

**처리 내용:**
1. ✅ `cti-event` 이벤트 수신
2. ✅ 이벤트 타입 확인: `event.EventType === 'INCOMING_CALL'`
3. ✅ 상태 업데이트:
   - `currentCall = event` (현재 통화로 설정)
   - `events = [event, ...prev.events]` (이벤트 목록에 추가)
4. ✅ 브라우저 알림 (선택사항)

**중요 포인트:**
- `INCOMING_CALL` 이벤트가 오면 `currentCall` 상태가 업데이트됩니다
- 이 상태가 CTI Panel에 반영됩니다

---

### 6단계: CTI Panel에 표시
```
         │
         ▼
┌─────────────────┐
│  CTI Panel      │
│  컴포넌트       │
│                 │
│  currentCall    │
│  표시           │
│                 │
│  📞 010-xxx     │
│  🕐 12:00:00    │
│  👤 환자 정보    │
└─────────────────┘
```

**표시 내용:**
- ✅ 전화번호: `currentCall.PhoneNumber`
- ✅ 시간: `currentCall.Timestamp`
- ✅ 환자 정보 (매칭된 경우)

---

## ⚠️ 문제 발생 지점

### 문제 1: Socket.IO 서버 미초기화
```
[2단계] Next.js API
    │
    │ global.io 확인
    │ ❌ null 또는 undefined
    │
    ▼
"⚠️ Socket.IO 서버가 초기화되지 않았습니다"
    │
    ▼
브로드캐스트 실패
```

**해결 방법:**
- 브라우저에서 `http://localhost:3001/api/socket` 접속하여 Socket.IO 서버 초기화

---

### 문제 2: 브라우저 클라이언트 미연결
```
[3단계] Socket.IO Server
    │
    │ 연결된 클라이언트 수 확인
    │ ❌ 0명
    │
    ▼
"⚠️ 연결된 클라이언트가 없습니다!"
    │
    ▼
브로드캐스트하지만 받을 클라이언트 없음
```

**해결 방법:**
- 브라우저 페이지 새로고침
- 브라우저 콘솔에서 Socket.IO 연결 오류 확인

---

### 문제 3: CID 수신 자체가 안 됨
```
[1단계] CTI Bridge
    │
    │ ❌ 전화가 오지 않음
    │ ❌ CTI Bridge 미실행
    │ ❌ 네트워크 오류
    │
    ▼
[2단계]로 전달되지 않음
```

**해결 방법:**
- CTI Bridge 프로그램 실행 확인
- 실제 전화 발신 확인
- CTI Bridge 콘솔에서 오류 확인

---

## ✅ 정상 동작 시 로그 예시

### Next.js 서버 터미널:
```
═══════════════════════════════════════
[1단계] CID 수신 API 호출됨
═══════════════════════════════════════
🔔 CID 수신 데이터: {
  발신번호: '010-1234-5678',
  수신번호: '031-567-2278',
  시각: '2024-01-01 12:00:00'
}
[2단계] Socket.IO 서버 상태 확인
  - global.io 존재: true
  - 연결된 클라이언트 수: 1
[3단계] CTI 이벤트 생성 완료
[4단계] 브로드캐스트 준비
  ✓ Socket.IO 브로드캐스트 완료
```

### 브라우저 콘솔:
```
═══════════════════════════════════════
[클라이언트] Socket.IO 연결 성공!
═══════════════════════════════════════
═══════════════════════════════════════
[클라이언트] CTI 이벤트 수신됨
═══════════════════════════════════════
[클라이언트] 이벤트 타입: INCOMING_CALL
🔔 [클라이언트] 전화 착신 처리: 010-1234-5678 → 031-567-2278
  ✓ currentCall 상태 업데이트 완료
```

---

## 🔍 문제 진단 체크리스트

각 단계별로 확인:

1. **1단계 확인:**
   - [ ] CTI Bridge가 실행 중인가?
   - [ ] CTI Bridge 콘솔에 "CID 수신 성공!" 메시지가 보이는가?
   - [ ] "Node.js 전송 성공!" 메시지가 보이는가?

2. **2단계 확인:**
   - [ ] Next.js 서버 터미널에 "[1단계] CID 수신 API 호출됨" 로그가 보이는가?
   - [ ] "global.io 존재: true" 메시지가 보이는가?
   - [ ] "브로드캐스트 완료" 메시지가 보이는가?

3. **3단계 확인:**
   - [ ] Socket.IO 서버가 초기화되었는가? (`/api/socket` 접속했는가?)
   - [ ] "연결된 클라이언트 수: 1" 이상인가?

4. **4단계 확인:**
   - [ ] 브라우저 콘솔에 "Socket.IO 연결 성공!" 메시지가 보이는가?
   - [ ] "CTI 세션 참가 확인" 메시지가 보이는가?

5. **5단계 확인:**
   - [ ] 브라우저 콘솔에 "CTI 이벤트 수신됨" 메시지가 보이는가?
   - [ ] "currentCall 상태 업데이트 완료" 메시지가 보이는가?

6. **6단계 확인:**
   - [ ] CTI Panel에 전화번호가 표시되는가?

---

## 📝 요약

**전체 흐름:**
```
전화 발생 
  → CTI Bridge CID 수신 
  → Next.js API POST 요청 
  → Socket.IO 서버 브로드캐스트 
  → 브라우저 클라이언트 수신 
  → React 상태 업데이트 
  → CTI Panel 화면 표시
```

**핵심 체크포인트:**
1. Socket.IO 서버 초기화 (`/api/socket`)
2. 브라우저 클라이언트 연결 (페이지 로드 시 자동)
3. 실제 CID 수신 (CTI Bridge에서 POST 요청)
4. 브로드캐스트 성공 (연결된 클라이언트 있음)

각 단계에서 로그를 확인하면 어디서 문제가 발생하는지 정확히 파악할 수 있습니다!


